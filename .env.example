# Supabase Free credentials (get from supabase.com → your project → Settings → API)
# Without these the app runs in local demo mode on the device.
VITE_SUPABASE_URL=https://xyzcompany.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key-here
